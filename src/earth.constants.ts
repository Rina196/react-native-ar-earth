import type { Vec3Tuple } from './earth.types';

export const DEFAULT_MIN_SCALE = 0.5;
export const DEFAULT_MAX_SCALE = 3;
export const DEFAULT_SPHERE_RADIUS = 0.5;
export const DEFAULT_SPHERE_ROTATION: Vec3Tuple = [0, 0, 180];
export const MARKER_SURFACE_OFFSET = 0.002;
